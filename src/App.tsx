import React, {type ReactNode} from "react";
import './App.css';

const App = (): ReactNode => (
    <div className="App">
        <p>
          Edit <code>src/App.js</code> and save to reload.
        </p>
    </div>
);

export default App;
